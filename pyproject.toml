[project]
name = "amd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-api-python-client>=2.0.0",
    "google-auth>=2.0.0",
    "python-dotenv>=1.0.0",
    "structlog>=24.0.0",
]
